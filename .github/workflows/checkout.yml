name: Checkout
on: [push]

jobs: 
    checkout-action: 
        runs-on: ubuntu-latest
        steps: 
            - name: Checkout
              uses: actions/checkout@v4.1.1
            - name: list files 
              run: ls -a
        